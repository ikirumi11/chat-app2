(()=>{'use strict';
if(window.__whiteboardLoaded)return;window.__whiteboardLoaded=true;
const fileInput=()=>document.getElementById('fileInput');
const attachBtn=()=>document.getElementById('attachBtn');
const sendBtn=()=>document.getElementById('sendBtn');
const canvas=document.createElement('canvas');canvas.width=1200;canvas.height=675;canvas.style.cssText='display:block;width:100%;height:auto;background:#fff;touch-action:none;cursor:crosshair;border-radius:8px';
const style=document.createElement('style');style.textContent=`.wb-overlay{position:fixed;inset:0;z-index:30000;display:none;align-items:center;justify-content:center;background:#000b;padding:20px}.wb-overlay.show{display:flex}.wb-box{width:min(1000px,96vw);max-height:94vh;background:#20242b;color:#fff;border:1px solid #fff3;border-radius:12px;display:flex;flex-direction:column;overflow:hidden;box-shadow:0 20px 70px #000b}.wb-head{display:flex;align-items:center;gap:10px;padding:12px 15px;border-bottom:1px solid #fff2}.wb-head b{flex:1}.wb-tools{display:flex;gap:7px;align-items:center;flex-wrap:wrap;padding:9px;border-bottom:1px solid #fff2}.wb-tools button,.wb-tools input{height:34px}.wb-tools button{padding:0 10px;border:1px solid #fff2;background:#fff1;color:#fff;border-radius:7px;cursor:pointer}.wb-tools input[type=color]{width:40px;border:0;background:none}.wb-tools input[type=range]{width:110px}.wb-canvas-wrap{padding:12px;overflow:auto}.wb-actions{display:flex;gap:8px;padding:10px;border-top:1px solid #fff2}.wb-actions button{flex:1;padding:10px;border-radius:8px;border:1px solid #fff2;background:#fff1;color:#fff;cursor:pointer;font-weight:600}.wb-close{border:0!important;background:none!important;font-size:22px}.wb-eraser.on{outline:2px solid #fff}`;document.head.appendChild(style);
const overlay=document.createElement('div');overlay.className='wb-overlay';overlay.innerHTML='<div class="wb-box"><div class="wb-head"><b>✏️ Whiteboard</b><span>Draw offline, then send as an image</span><button class="wb-close" id="wbClose">×</button></div><div class="wb-tools"><button id="wbPen">✏️ Pen</button><button id="wbErase">🧹 Eraser</button><label>Size <input id="wbSize" type="range" min="1" max="80" value="6"></label><input id="wbColor" type="color" value="#000000"><button id="wbClear">Clear</button></div><div class="wb-canvas-wrap" id="wbWrap"></div><div class="wb-actions"><button id="wbCancel">Close</button><button id="wbSend">Send Image</button></div></div>';document.body.appendChild(overlay);overlay.querySelector('#wbWrap').appendChild(canvas);
const ctx=canvas.getContext('2d',{alpha:false});ctx.fillStyle='#fff';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.lineCap='round';ctx.lineJoin='round';let drawing=false,eraser=false,lastX=0,lastY=0;
function pos(e){const r=canvas.getBoundingClientRect();return{x:(e.clientX-r.left)*canvas.width/r.width,y:(e.clientY-r.top)*canvas.height/r.height}}
function down(e){e.preventDefault();drawing=true;const p=pos(e);lastX=p.x;lastY=p.y;ctx.beginPath();ctx.arc(p.x,p.y,Number(overlay.querySelector('#wbSize').value)/2,0,Math.PI*2);ctx.fillStyle=eraser?'#fff':overlay.querySelector('#wbColor').value;ctx.fill()}
function move(e){if(!drawing)return;e.preventDefault();const p=pos(e),size=Number(overlay.querySelector('#wbSize').value);ctx.strokeStyle=eraser?'#fff':overlay.querySelector('#wbColor').value;ctx.lineWidth=size;ctx.beginPath();ctx.moveTo(lastX,lastY);ctx.lineTo(p.x,p.y);ctx.stroke();lastX=p.x;lastY=p.y}
function up(){drawing=false}
canvas.addEventListener('pointerdown',down);canvas.addEventListener('pointermove',move);canvas.addEventListener('pointerup',up);canvas.addEventListener('pointercancel',up);canvas.addEventListener('pointerleave',up);
overlay.querySelector('#wbErase').onclick=()=>{eraser=!eraser;overlay.querySelector('#wbErase').classList.toggle('on',eraser)};overlay.querySelector('#wbPen').onclick=()=>{eraser=false;overlay.querySelector('#wbErase').classList.remove('on')};overlay.querySelector('#wbClear').onclick=()=>{ctx.fillStyle='#fff';ctx.fillRect(0,0,canvas.width,canvas.height)};
function close(){overlay.classList.remove('show')};function open(){overlay.classList.add('show')}
overlay.querySelector('#wbClose').onclick=close;overlay.querySelector('#wbCancel').onclick=close;
async function send(){const input=fileInput();if(!input){alert('Chat file input was not found.');return}const blob=await new Promise(r=>canvas.toBlob(r,'image/png'));if(!blob)return;if(blob.size>5*1024*1024){alert('Drawing is too large.');return}const file=new File([blob],'whiteboard.png',{type:'image/png'});try{const dt=new DataTransfer();dt.items.add(file);input.files=dt.files;input.dispatchEvent(new Event('change',{bubbles:true}));close();setTimeout(()=>{sendBtn()?.click()},100)}catch(e){console.error(e);alert('Could not prepare the drawing for sending.')}}
overlay.querySelector('#wbSend').onclick=send;
const button=document.createElement('button');button.type='button';button.className='attach-btn';button.id='whiteboardBtn';button.title='Whiteboard';button.setAttribute('aria-label','Whiteboard');button.textContent='✏️';const a=attachBtn();if(a?.parentNode)a.parentNode.insertBefore(button,a);else document.body.appendChild(button);button.onclick=open;
const pollScript=document.createElement('script');pollScript.src='polls.js';pollScript.async=false;pollScript.onload=()=>console.log('Polls loaded');pollScript.onerror=e=>console.warn('Polls could not load',e);document.head.appendChild(pollScript);
})();