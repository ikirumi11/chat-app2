(()=>{window.ChatMultiplayerGames.register({id:'typing2',name:'Typing Race',icon:'⌨️',minPlayers:1,maxPlayers:4,render(el,a){const g=a.game;g.data??={text:'The quick brown fox jumps over the lazy dog.',done:{}};const d=g.data;el.innerHTML=`<div class="g3-center"><h2>⌨️ Typing Race</h2><p>${d.text}</p><textarea id="type" class="g3-input" style="width:100%;min-height:100px"></textarea><button class="g3-btn" id="go">Finish</button><p>${Object.entries(d.done).map(([k,v])=>`${g.players.find(p=>p.deviceId===k)?.username||'Player'}: ${v}s`).join(' · ')}</p></div>`;el.querySelector('#go').onclick=async()=>{if(d.done[a.me])return;const t=el.querySelector('#type').value;if(t===d.text)d.done[a.me]=Math.round(performance.now()/10)/100;await a.publish(g)}}});})();