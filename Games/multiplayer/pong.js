(()=>{window.ChatMultiplayerGames.register({id:'pong',name:'Pong',icon:'🏓',minPlayers:2,maxPlayers:2,render(el,a){const g=a.game;g.data??={scores:[0,0],ball:0};el.innerHTML=`<div class="g3-center"><h2>🏓 Pong</h2><p>Fast turn-based rally version.</p><div class="g3-big">${g.data.scores[0]} — ${g.data.scores[1]}</div><button class="g3-btn" id="hit">🏓 Hit</button></div>`;el.querySelector('#hit').onclick=async()=>{g.data.ball++;if(Math.random()<.18){g.data.scores[g.data.ball%2]++;g.data.ball=0}await a.publish(g)}}});})();