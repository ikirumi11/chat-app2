(()=>{window.ChatMultiplayerGames.register({id:'boss',name:'Boss Battle',icon:'👹',minPlayers:2,maxPlayers:6,render(el,a){const g=a.game;g.data??={hp:1000000,max:1000000,damage:{}};const d=g.data;el.innerHTML=`<div class="g3-center"><h2>👹 Boss Battle</h2><div class="g3-big">${d.hp.toLocaleString()} HP</div><p>Team damage: ${Object.values(d.damage).reduce((x,y)=>x+y,0).toLocaleString()}</p><button class="g3-btn" id="hit">⚔️ Attack +${1000+Math.floor(Math.random()*9000)}</button></div>`;el.querySelector('#hit').onclick=async()=>{const n=1000+Math.floor(Math.random()*9000);d.damage[a.me]=(d.damage[a.me]||0)+n;d.hp=Math.max(0,d.hp-n);await a.publish(g)}}});})();