(()=>{window.ChatMultiplayerGames.register({id:'color',name:'Color Clash',icon:'🎨',minPlayers:1,maxPlayers:4,render(el,a){const g=a.game;g.data??={scores:{},target:'RED'};const colors=['RED','BLUE','GREEN','YELLOW'];el.innerHTML=`<div class="g3-center"><h2>🎨 Color Clash</h2><div class="g3-big">${g.data.target}</div>${colors.map(c=>`<button class="g3-btn" data-c="${c}">${c}</button>`).join('')}<p>${g.players.map(p=>`${p.username}: ${g.data.scores[p.deviceId]||0}`).join(' · ')}</p></div>`;el.querySelectorAll('[data-c]').forEach(b=>b.onclick=async()=>{if(b.dataset.c===g.data.target)g.data.scores[a.me]=(g.data.scores[a.me]||0)+1;g.data.target=colors[Math.floor(Math.random()*colors.length)];await a.publish(g)})}})();