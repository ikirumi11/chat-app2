(()=>{window.ChatMultiplayerGames.register({id:'rpsTournament',name:'RPS Tournament',icon:'🏆',minPlayers:2,maxPlayers:4,render(el,a){const g=a.game;g.data??={scores:{},round:1,choices:{}};const d=g.data;el.innerHTML=`<div class="g3-center"><h2>🏆 RPS Tournament</h2><p>Round ${d.round} · Choose your move.</p>${['✊','✋','✌️'].map(x=>`<button class="g3-btn" data-v="${x}">${x}</button>`).join('')}<p>${g.players.map(p=>`${p.username}: ${d.scores[p.deviceId]||0}`).join(' · ')}</p></div>`;el.querySelectorAll('[data-v]').forEach(b=>b.onclick=async()=>{d.choices[a.me]=b.dataset.v;if(Object.keys(d.choices).length===g.players.length){Object.values(d.choices).forEach((x,i)=>{if(x===Object.values(d.choices)[0])d.scores[g.players[i].deviceId]=(d.scores[g.players[i].deviceId]||0)+1});d.choices={};d.round++}await a.publish(g)})}})();