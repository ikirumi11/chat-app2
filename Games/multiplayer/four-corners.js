(()=>{window.ChatMultiplayerGames.register({id:'four',name:'Four Corners',icon:'🟡',minPlayers:1,maxPlayers:4,render(el,a){const g=a.game;g.data??={scores:{},safe:1};const d=g.data;el.innerHTML=`<div class="g3-center"><h2>🟡 Four Corners</h2><p>Choose a corner. One corner is safe.</p>${[1,2,3,4].map(n=>`<button class="g3-btn" data-c="${n}">Corner ${n}</button>`).join('')}<p>${g.players.map(p=>`${p.username}: ${d.scores[p.deviceId]||0}`).join(' · ')}</p></div>`;el.querySelectorAll('[data-c]').forEach(b=>b.onclick=async()=>{const c=+b.dataset.c;d.safe=1+Math.floor(Math.random()*4);if(c===d.safe)d.scores[a.me]=(d.scores[a.me]||0)+1;await a.publish(g)})}})();