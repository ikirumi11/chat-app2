(()=>{window.ChatMultiplayerGames.register({id:'dots',name:'Dots',icon:'🔵',minPlayers:1,maxPlayers:4,render(el,a){const g=a.game;g.data??={scores:{}};el.innerHTML=`<div class="g3-center"><h2>🔵 Dots</h2><p>Claim dots for your score.</p><div class="g3-grid" style="grid-template-columns:repeat(5,1fr)">${Array.from({length:25},(_,i)=>`<button class="g3-cell" data-i="${i}">${g.data.board?.[i]||'·'}</button>`).join('')}</div><p>${g.players.map(p=>`${p.username}: ${g.data.scores[p.deviceId]||0}`).join(' · ')}</p></div>`;g.data.board??=Array(25).fill('');el.querySelectorAll('[data-i]').forEach(b=>b.onclick=async()=>{const i=+b.dataset.i;if(g.data.board[i])return;g.data.board[i]='🔵';g.data.scores[a.me]=(g.data.scores[a.me]||0)+1;await a.publish(g)})}})();