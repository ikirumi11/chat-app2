(()=>{window.ChatMultiplayerGames.register({id:'scramble',name:'Word Scramble',icon:'🔤',minPlayers:2,maxPlayers:6,render(el,a){const g=a.game;g.data??={word:'APPLE',scramble:'PPALE',scores:{}};const d=g.data;el.innerHTML=`<div class="g3-center"><h2>🔤 Word Scramble</h2><div class="g3-big">${d.scramble}</div><input id="ans" class="g3-input" placeholder="Your answer"><button class="g3-btn" id="go">Submit</button><p>${g.players.map(p=>`${p.username}: ${d.scores[p.deviceId]||0}`).join(' · ')}</p></div>`;el.querySelector('#go').onclick=async()=>{if(el.querySelector('#ans').value.trim().toUpperCase()===d.word)d.scores[a.me]=(d.scores[a.me]||0)+1;const w=['APPLE','DRAGON','FOREST','PLANET','RABBIT'];d.word=w[Math.floor(Math.random()*w.length)];d.scramble=[...d.word].sort(()=>Math.random()-.5).join('');await a.publish(g)}}});})();