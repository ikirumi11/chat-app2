(()=>{window.ChatMultiplayerGames.register({id:'quickdraw',name:'Quick Draw',icon:'🏁',minPlayers:2,maxPlayers:6,render(el,a){const g=a.game;g.data??={scores:{}};el.innerHTML=`<div class="g3-center"><h2>🏁 Quick Draw</h2><p>First to react wins the round.</p><button class="g3-btn" data-x="1" style="font-size:32px;padding:28px">DRAW!</button><p>Scores: ${g.players.map(p=>`${p.username} ${g.data.scores[p.deviceId]||0}`).join(' · ')}</p></div>`;el.querySelector('[data-x]').onclick=async()=>{g.data.scores[a.me]=(g.data.scores[a.me]||0)+1;await a.publish(g)}}});})();