(()=>{window.ChatMultiplayerGames.register({id:'hangman',name:'Hangman',icon:'🔤',minPlayers:2,maxPlayers:4,render(el,a){const g=a.game;g.data??={word:'COMPUTER',guessed:[],attempts:6};const d=g.data;const shown=[...d.word].map(c=>d.guessed.includes(c)?c:'_').join(' ');el.innerHTML=`<div class="g3-center"><h2>🔤 Hangman</h2><div class="g3-big">${shown}</div><p>Attempts: ${d.attempts}</p><div>${'ABCDEFGHIJKLMNOPQRSTUVWXYZ'.split('').map(c=>`<button class="g3-btn" data-l="${c}">${c}</button>`).join('')}</div></div>`;el.querySelectorAll('[data-l]').forEach(b=>b.onclick=async()=>{const c=b.dataset.l;if(d.guessed.includes(c))return;d.guessed.push(c);if(!d.word.includes(c))d.attempts--;await a.publish(g)})}})();