/* Global Chat Background — shared file upload */
(() => {
  'use strict';
  const API='/api/global-background', KEY='chatGlobalBackground', MAX_DIM=1600, MAX_BYTES=900*1024;
  function applyBackground(url){let style=document.getElementById('global-chat-background-style');if(!style){style=document.createElement('style');style.id='global-chat-background-style';document.head.appendChild(style)}const safe=String(url||'').replace(/\\/g,'\\\\').replace(/"/g,'\\"').replace(/</g,'%3C');style.textContent=safe?`.messages{background-image:linear-gradient(rgba(10,12,16,.4),rgba(10,12,16,.4)),url("${safe}") !important;background-size:cover !important;background-position:center !important;background-repeat:no-repeat !important;background-attachment:fixed !important;}`:'.messages{background-image:none !important;}';}
  async function load(){try{const r=await fetch(API,{cache:'no-store'});if(r.ok){const d=await r.json();if(d?.url){localStorage.setItem(KEY,d.url);applyBackground(d.url);return}}}catch(e){}applyBackground(localStorage.getItem(KEY)||'')}
  function resizeImage(file){return new Promise((resolve,reject)=>{const reader=new FileReader();reader.onerror=reject;reader.onload=()=>{const img=new Image();img.onerror=reject;img.onload=()=>{const scale=Math.min(1,MAX_DIM/img.width,MAX_DIM/img.height),w=Math.max(1,Math.round(img.width*scale)),h=Math.max(1,Math.round(img.height*scale));const c=document.createElement('canvas');c.width=w;c.height=h;c.getContext('2d').drawImage(img,0,0,w,h);let q=.82,data=c.toDataURL('image/jpeg',q);while(data.length>MAX_BYTES*1.37&&q>.45){q-=.07;data=c.toDataURL('image/jpeg',q)}resolve(data)};img.src=reader.result};reader.readAsDataURL(file)})}
  async function save(data,status){status.textContent='Saving background…';try{const r=await fetch(API,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({url:data})}),d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||'Could not save background');localStorage.setItem(KEY,d.url||data);applyBackground(d.url||data);status.textContent='✓ Global background updated for everyone.'}catch(e){status.textContent='✕ '+e.message}}
  async function clear(status){status.textContent='Removing background…';try{const r=await fetch(API,{method:'DELETE'});if(!r.ok)throw Error('Could not remove background');localStorage.removeItem(KEY);applyBackground('');status.textContent='✓ Global background removed.'}catch(e){status.textContent='✕ '+e.message}}
  function setup(){const panel=document.querySelector('#settingsOverlay .panel-body');if(!panel||document.getElementById('globalBackgroundSetting'))return;const category=document.createElement('div');category.className='category';category.id='globalBackgroundSetting';category.innerHTML=`<button class="category-title"><span>🌄 Global Chat Background</span><span>⌄</span></button><div class="category-body"><div class="setting"><label>Background image file</label><input id="globalBackgroundFile" type="file" accept="image/png,image/jpeg,image/webp,image/gif"><small style="color:#89919d;display:block;margin-top:6px">Choose an image from your computer. It will be shared with everyone.</small><img id="globalBackgroundPreview" style="display:none;width:100%;height:130px;object-fit:cover;border-radius:10px;margin-top:10px;border:1px solid #343a44" alt="Background preview"></div><div class="setting"><button class="save-btn" id="saveGlobalBackground" disabled>Set global background</button><button class="game-btn" id="clearGlobalBackground" style="margin-top:8px;width:100%">Clear background</button><div id="globalBackgroundStatus" style="color:#929aa5;font-size:12px;margin-top:9px;min-height:18px"></div></div></div>`;const appearance=[...panel.querySelectorAll('.category')].find(x=>x.querySelector('.category-title')?.textContent.includes('Appearance'));if(appearance)appearance.after(category);else panel.prepend(category);category.querySelector('.category-title').onclick=()=>category.classList.toggle('open');let selected=null;const input=category.querySelector('#globalBackgroundFile'),preview=category.querySelector('#globalBackgroundPreview'),saveBtn=category.querySelector('#saveGlobalBackground'),status=category.querySelector('#globalBackgroundStatus');input.onchange=async()=>{const file=input.files?.[0];if(!file)return;status.textContent='Preparing image…';try{selected=await resizeImage(file);preview.src=selected;preview.style.display='block';saveBtn.disabled=false;status.textContent='Ready to save.'}catch(e){selected=null;saveBtn.disabled=true;status.textContent='Could not read that image.'}};saveBtn.onclick=()=>selected&&save(selected,status);category.querySelector('#clearGlobalBackground').onclick=()=>clear(status)}
  function boot(){load();setup();new MutationObserver(setup).observe(document.documentElement,{childList:true,subtree:true})}
  document.readyState==='loading'?document.addEventListener('DOMContentLoaded',boot):boot();
})();
