(()=>{'use strict';const S=window.ChatSoloGames;S.dodger=(el)=>{el.innerHTML=`<div class="soloGame"><div class="soloHUD"><b>☄️ Space Dodger</b><span id="score">0</span><span id="best">Best ${localStorage.dodgeBest||0}</span></div><canvas width="600" height="650" class="soloCanvas"></canvas><p class="soloHint">WASD / arrows · Survive as long as possible</p><button class="g3-btn" id="restart">Restart</button></div>`;const c=el.querySelector('canvas'),x=c.getContext('2d');let p,rocks,score,over,last,raf;const reset=()=>{p={x:300,y:570};rocks=[];score=0;over=false;last=0;cancelAnimationFrame(raf);raf=requestAnimationFrame(loop)};const loop=t=>{let dt=Math.min(.03,(t-last)/1000||.016);last=t;if(Math.random()<dt*2.5)rocks.push({x:Math.random()*570+15,y:-30,r:10+Math.random()*22,v:130+score*3+Math.random()*90});rocks.forEach(r=>{r.y+=r.v*dt});rocks=rocks.filter(r=>r.y<700);if(rocks.some(r=>Math.hypot(r.x-p.x,r.y-p.y)<r.r+15))over=true;score+=dt;draw();if(!over)raf=requestAnimationFrame(loop)};const draw=()=>{x.fillStyle='#050711';x.fillRect(0,0,600,650);x.fillStyle='#fff';for(let i=0;i<80;i++)x.fillRect((i*83)%600,(i*137+Math.floor(score*30))%650,2,2);rocks.forEach(r=>{x.fillStyle='#b6c0d0';x.beginPath();x.arc(r.x,r.y,r.r,0,7);x.fill()});x.fillStyle='#54b8ff';x.beginPath();x.moveTo(p.x,p.y-22);x.lineTo(p.x-17,p.y+18);x.lineTo(p.x,p.y+10);x.lineTo(p.x+17,p.y+18);x.closePath();x.fill();el.querySelector('#score').textContent=Math.floor(score);if(over){localStorage.dodgeBest=Math.max(+localStorage.dodgeBest||0,Math.floor(score));x.fillStyle='#000a';x.fillRect(0,0,600,650);x.fillStyle='#fff';x.font='bold 38px system-ui';x.textAlign='center';x.fillText('SHIP LOST',300,300);x.font='20px system-ui';x.fillText(`Survival ${Math.floor(score)}s`,300,335)}};let keys={};const kd=e=>keys[e.key.toLowerCase()]=1,ku=e=>delete keys[e.key.toLowerCase()];const move=()=>{let dx=(keys.d||keys.arrowright?1:0)-(keys.a||keys.arrowleft?1:0),dy=(keys.s||keys.arrowdown?1:0)-(keys.w||keys.arrowup?1:0);p.x=Math.max(20,Math.min(580,p.x+dx*300/60));p.y=Math.max(20,Math.min(630,p.y+dy*300/60));if(!over)requestAnimationFrame(move)};window.addEventListener('keydown',kd);window.addEventListener('keyup',ku);el.querySelector('#restart').onclick=reset;reset();move();return()=>{cancelAnimationFrame(raf);window.removeEventListener('keydown',kd);window.removeEventListener('keyup',ku)}}})();