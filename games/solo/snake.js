(()=>{'use strict';const S=window.ChatSoloGames;S.snake=(el)=>{el.innerHTML=`<div class="soloGame"><div class="soloHUD"><b>🐍 Snake</b><span id="score">Score 0</span><span id="best">Best ${localStorage.snakeBest||0}</span></div><canvas width="600" height="600" class="soloCanvas"></canvas><p class="soloHint">WASD / Arrow keys · Swipe on mobile</p><button class="g3-btn" id="restart">Restart</button></div>`;const c=el.querySelector('canvas'),x=c.getContext('2d'),n=20,cell=30;let snake,dir,next,food,score,running,timer;const reset=()=>{snake=[{x:10,y:10},{x:9,y:10},{x:8,y:10}];dir={x:1,y:0};next=dir;score=0;food=spawn();running=true;clearInterval(timer);timer=setInterval(tick,115);draw()};const spawn=()=>{let f;do f={x:Math.floor(Math.random()*n),y:Math.floor(Math.random()*n)};while(snake&&snake.some(p=>p.x===f.x&&p.y===f.y));return f};const tick=()=>{dir=next;let h={x:snake[0].x+dir.x,y:snake[0].y+dir.y};if(h.x<0||h.x>=n||h.y<0||h.y>=n||snake.some(p=>p.x===h.x&&p.y===h.y)){running=false;clearInterval(timer);localStorage.snakeBest=Math.max(+localStorage.snakeBest||0,score);draw();return}snake.unshift(h);if(h.x===food.x&&h.y===food.y){score++;food=spawn();if(score%5===0){clearInterval(timer);timer=setInterval(tick,Math.max(55,115-score*2))}}else snake.pop();draw()};const draw=()=>{x.clearRect(0,0,600,600);x.fillStyle='#10151d';x.fillRect(0,0,600,600);x.strokeStyle='#ffffff08';for(let i=0;i<=n;i++){x.beginPath();x.moveTo(i*cell,0);x.lineTo(i*cell,600);x.moveTo(0,i*cell);x.lineTo(600,i*cell);x.stroke()}x.fillStyle='#ff5964';x.beginPath();x.arc(food.x*cell+15,food.y*cell+15,11,0,7);x.fill();snake.forEach((p,i)=>{x.fillStyle=i?'#54e38e':'#8cffb5';x.fillRect(p.x*cell+2,p.y*cell+2,26,26)});el.querySelector('#score').textContent=`Score ${score}`;if(!running){x.fillStyle='#000a';x.fillRect(0,0,600,600);x.fillStyle='white';x.font='bold 34px system-ui';x.textAlign='center';x.fillText('Game Over',300,285);x.font='20px system-ui';x.fillText(`Score ${score}`,300,320)}};const key=e=>{const m={ArrowUp:{x:0,y:-1},w:{x:0,y:-1},ArrowDown:{x:0,y:1},s:{x:0,y:1},ArrowLeft:{x:-1,y:0},a:{x:-1,y:0},ArrowRight:{x:1,y:0},d:{x:1,y:0}}[e.key];if(m&&m.x!==-dir.x&&m.y!==-dir.y){next=m;e.preventDefault()}};window.addEventListener('keydown',key);el.querySelector('#restart').onclick=reset;reset();return()=>{clearInterval(timer);window.removeEventListener('keydown',key)}}})();