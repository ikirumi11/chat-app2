(()=>{'use strict';const S=window.ChatSoloGames;S['2048']=(el)=>{el.innerHTML=`<div class="soloGame"><div class="soloHUD"><b>🔢 2048</b><span id="score">Score 0</span><span>Best <i id="best">${localStorage.g2048||0}</i></span></div><div id="board" class="g2048"></div><p class="soloHint">Arrow keys / swipe · Combine equal tiles</p><button class="g3-btn" id="restart">New Game</button></div>`;const b=el.querySelector('#board');let a,score;const add=()=>{let q=[];a.forEach((v,i)=>!v&&q.push(i));if(q.length){let i=q[Math.floor(Math.random()*q.length)];a[i]=Math.random()<.9?2:4}};const reset=()=>{a=Array(16).fill(0);score=0;add();add();draw()};const move=d=>{let old=a.join(),out=Array(16).fill(0);const lines=[];for(let r=0;r<4;r++)lines.push([0,1,2,3].map(c=>r*4+c));for(let c=0;c<4;c++)lines.push([0,1,2,3].map(r=>r*4+c));if(d[1])lines.splice(0,4);let L=d[0]?lines.slice(0,4):lines.slice(4);if(d[0]<0)L=L.map(z=>z.reverse());if(d[1]<0)L=L.map(z=>z.reverse());L.forEach(line=>{let vals=line.map(i=>a[i]).filter(Boolean),res=[];for(let i=0;i<vals.length;i++){if(vals[i]===vals[i+1]){res.push(vals[i]*2);score+=vals[i]*2;i++}else res.push(vals[i])}while(res.length<4)res.push(0);line.forEach((i,k)=>out[i]=res[k])});a=out;if(old!==a.join()){add();draw()}};const draw=()=>{b.innerHTML=a.map(v=>`<div class="tile t${v}">${v||''}</div>`).join('');el.querySelector('#score').textContent='Score '+score;localStorage.g2048=Math.max(+localStorage.g2048||0,score)};const key=e=>{const m={ArrowLeft:[-1,0],ArrowRight:[1,0],ArrowUp:[0,-1],ArrowDown:[0,1]}[e.key];if(m){e.preventDefault();move(m)}};window.addEventListener('keydown',key);el.querySelector('#restart').onclick=reset;reset();return()=>window.removeEventListener('keydown',key)}})();